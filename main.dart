import 'package:flutter/material.dart';
import 'package:My_App/ui/code.dart';

void main() => runApp(App());

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return AudioScreen();
  }
}
